namespace MyFinanceTracker.Core.Enums
{
    /// <summary>
    /// Supported currency codes (ISO 4217). Stored as string in the database (e.g. "USD", "EUR").
    /// </summary>
    public enum CurrencyCode
    {
        USD,
        EUR
    }
}
